# قانون کامیت و پوش

هر تغییری در این پروژه باید کامیت شود و به **هر دو** ریموت زیر پوش شود:

- GitHub: `origin` → https://github.com/omidazg/TapsellChallengeProgrammerDay
- GitLab (تپسل): `tapsell` → https://git.tapsell.ir/os/internaltools/aifoundershackaton

```bash
git push origin main
git push tapsell main
```

هیچ تغییری نباید فقط روی یکی از این دو ریموت باقی بماند.
